<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="theme-color" content="#05080c" />
  <link rel="manifest" href="./manifest.json" />
  <link rel="apple-touch-icon" href="./assets/icon-192.png" />
  <title>STATIC ROOM // THE WIRED</title>
  <link rel="stylesheet" href="./styles.css" />
  <style>
    /* static page specific */
    .static-shell{
      position: relative;
      min-height: 100vh;
      overflow: hidden;
    }
    .static-stage{
      position: fixed;
      inset: 0;
      z-index: 1;
    }
    #staticCanvas{
      width: 100%;
      height: 100%;
      display: block;
      image-rendering: pixelated;
    }
    .static-overlay{
      position: fixed;
      inset: 0;
      pointer-events: none;
      z-index: 2;
    }
    .static-ui{
      position: fixed;
      left: 50%;
      bottom: 24px;
      transform: translateX(-50%);
      z-index: 3;
      width: min(920px, calc(100% - 24px));
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 10px;
      align-items: center;
    }
    .static-panel{
      border: 1px solid rgba(120,255,170,.22);
      background: rgba(0,0,0,.35);
      padding: 10px 12px;
      border-radius: 12px;
      backdrop-filter: blur(6px);
      box-shadow: 0 0 18px rgba(80,255,160,.08);
    }
    .static-title{
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace;
      letter-spacing: .08em;
      font-size: 12px;
      opacity: .9;
      margin-bottom: 6px;
    }
    .static-row{
      display: flex;
      gap: 8px;
      align-items: center;
      justify-content: space-between;
    }
    .static-row label{
      width: 100%;
      display: flex;
      gap: 8px;
      align-items: center;
      font-size: 12px;
      opacity: .9;
    }
    .static-row input[type="range"]{ width: 100%; }
    .static-hint{
      position: fixed;
      top: 70px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 3;
      width: min(920px, calc(100% - 24px));
      pointer-events: none;
      text-align: left;
      font-size: 12px;
      opacity: .85;
      letter-spacing: .06em;
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace;
      text-shadow: 0 0 10px rgba(80,255,160,.12);
    }
    .static-portrait{
      position: fixed;
      right: 14px;
      top: 88px;
      width: min(280px, 42vw);
      z-index: 2;
      opacity: .65;
      mix-blend-mode: screen;
      filter: contrast(1.1) saturate(.9);
      pointer-events: none;
      transform: translateZ(0);
    }
    .static-portrait img{
      width: 100%;
      height: auto;
      display: block;
      image-rendering: pixelated;
      filter: drop-shadow(0 0 14px rgba(80,255,160,.10));
    }
    @media (max-width: 860px){
      .static-ui{ grid-template-columns: 1fr; }
      .static-portrait{ opacity: .45; }
    }
  </style>
</head>
<body>
  <header class="topbar">
    <div class="brand">
      <div class="sig">PROTOCOL // STATIC ROOM</div>
      <div class="name" data-glitch="STATIC ROOM">STATIC ROOM</div>
    </div>
    <div class="top-actions">
      <a class="pill" href="./index.html">BACK</a>
      <button id="btnEnable" class="pill">ENABLE AUDIO</button>
      <button id="btnToggle" class="pill ghost">START</button>
    </div>
  </header>

  <div class="static-shell">
    <div class="static-stage">
      <canvas id="staticCanvas"></canvas>
    </div>

    <!-- 既存の雰囲気レイヤを流用 -->
    <div class="scanlines static-overlay"></div>
    <div class="vignette static-overlay"></div>
    <div class="dither static-overlay"></div>
    <div class="warp static-overlay"></div>
    <div class="glitch-layer static-overlay" aria-hidden="true"></div>

    <!-- “存在” -->
    <div class="static-portrait" aria-hidden="true">
      <!-- 既存の wired-girl.png を使う（差し替えOK） -->
      <img src="./assets/wired-girl.png" alt="" />
    </div>

    <div class="static-hint">
      do not panic. observe.<br/>
      audio requires user gesture. enable once. then start.
    </div>

    <div class="static-ui">
      <div class="static-panel">
        <div class="static-title">VISUAL</div>
        <div class="static-row">
          <label>INTENSITY <input id="vIntensity" type="range" min="0" max="100" value="72" /></label>
        </div>
        <div class="static-row" style="margin-top:8px;">
          <label>GLITCH <input id="vGlitch" type="range" min="0" max="100" value="35" /></label>
        </div>
      </div>

      <div class="static-panel">
        <div class="static-title">AUDIO</div>
        <div class="static-row">
          <label>VOLUME <input id="aVol" type="range" min="0" max="100" value="28" /></label>
        </div>
        <div class="static-row" style="margin-top:8px;">
          <label>TONE <input id="aTone" type="range" min="0" max="100" value="35" /></label>
        </div>
      </div>

      <div class="static-panel">
        <div class="static-title">MODE</div>
        <div class="static-row">
          <button id="btnBurst" class="pill ghost" style="width:100%;">BURST GLITCH</button>
        </div>
        <div class="static-row" style="margin-top:8px;">
          <button id="btnCalm" class="pill ghost" style="width:100%;">CALM DOWN</button>
        </div>
      </div>
    </div>
  </div>

  <!-- 既存の演出JS（あれば） -->
  <script type="module" src="./crt.js"></script>
  <script type="module" src="./ps1.js"></script>

  <!-- STATIC専用 -->
  <script type="module" src="./static.js"></script>
</body>
</html>
